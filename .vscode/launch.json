{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Android: Run On Connected Device",
      "type": "node-terminal",
      "request": "launch",
      "command": "${workspaceFolder}\\run-app.bat"
    }
  ]
}
